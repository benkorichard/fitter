High priority

- [x] Basic test coverage
  - API smoke tests for core flows: create session, log sets, finish session, summary/progress.

- [x] Data quality improvements
  - Add optional RPE/RIR fields per set.

- [x] Better progression analytics
  - Rolling bests (30/90 day), plateau detection, and new-PR highlights.

- [x] Program templates
  - Save reusable session/program templates (for ABA/BAB blocks) and clone quickly.

- [x] Calendar and consistency view
  - Weekly/monthly workout heatmap.

Medium priority

- [ ] API documentation polish
  - Improve schema examples and endpoint descriptions.

- [x] Import capability (CSV/JSON)
  - Add import with validation and dry-run preview.

- [x] Import flexibility improvements
  - Accept exercises-only JSON payloads (without requiring program/session rows).
  - Support importing from exercise export format directly.

- [x] Clarify Export/Import UX
  - Rename/restructure Export page and nav so import actions are not hidden under Export.
  - Consider split pages or a unified Data Management section.

- [x] Active workout UX refinement
  - Keyboard-first logging, duplicate previous set, and quick increment/decrement controls.

- [x] Lightweight observability
  - Request logging, simple error tracking, and health checks.

- [x] Program status persistence bug
  - In Program Edit, saving status (active/completed/paused) is not persisted and reverts to active.

- [x] Session deletion/cancel capability
  - Allow removing a workout session from a program (e.g., aborted due to injury/early stop).
  - Add confirmation UX to avoid accidental deletion.

- [x] Session archive lifecycle (preferred over hard delete)
  - Archive sessions to hide them from day-to-day dashboard while preserving analytics/history.
  - Support unarchive and duplicate archived session into a new active session/template.
  - Keep hard-delete as optional destructive action with strong confirmation.

- [x] Analytics protection for aborted sessions
  - Prevent very short/aborted workouts (e.g., 1 set then finish) from skewing progression analytics.
  - Options: mark session as aborted/invalid, minimum completed-set threshold, or exclude from trend calculations.

- [ ] Estimated calorie burn
  - Estimate calories from exercise type, external load, set/rep volume, and session duration.
  - Show estimate per workout session and aggregate per program/date range.
  - Display assumptions clearly (heuristic estimate, not medical-grade accuracy).
